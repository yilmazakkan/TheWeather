import { LogsFilterPipe } from './logs-filter.pipe';

describe('LogsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LogsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
